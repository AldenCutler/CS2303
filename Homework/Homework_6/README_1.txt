This program takes two matrices and adds them together using the overloaded + operator. 
It also allows for additions between matrices and floats, and overloads the << operator to be able to print directly to the terminal.

To compile: g++ -Wall run_matrix.cpp Matrix.cpp -lm –o runM
To run: ./runM